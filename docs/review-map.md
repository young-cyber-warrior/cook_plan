# Карта замечаний по коду

Замечания оставлены комментариями на русском прямо в коде. Правило работы: берём **одно** замечание, чиним, потом удаляем его комментарий, потом следующее.

Статусы: **закрыто** — фикс в коде, комментарий удалён · **в работе** · **ждёт** — комментарий на месте.

---

## Закрыто

| Замечание | Где было | Что сделано |
|---|---|---|
| «зачем столько геттеров, каждый раз запускается расчёт… (это касается всех сторов)» | `recipes-store.ts` над классом | Геттеры под `makeAutoObservable` уже `computed` — кэш есть, пока значение кто-то наблюдает (observer/reaction). Настоящие дыры закрыты: слой индексов-`computed` (`recipeRowById`, `ingredientsByRecipeId`, `itemRowsByListId`, `weekRowById`, `mealRowsByWeekDay`); экшены (`saveRecipe`, `refresh`, `generate`, `resizeWeek`) читают индексы, а не тяжёлые доменные массивы; `hasEdits` — `some` по группе вместо map+sort всего списка; `findBySlotCategory` — `find` по rows вместо построения Map всех рецептов с `JSON.parse` фото |
| «каждый раз при обращении к `hasEdits` проход по массиву» | `grocery-store.ts` | Считается по строкам активного списка, без сборки доменного `list` |
| «упростить всё в один цикл + вынести в функцию» | `recipes-store.ts` (`categories`) | `toCategories(rows, ownerId)` в `stores/mappers.ts` |
| «зачем все эти операции с `ingredientsByRecipe`» | `recipes-store.ts` (`recipes`) | `groupIngredientsByRecipe` — один проход строит группы, дальше O(1) на рецепт вместо фильтра по всем ингредиентам на каждый рецепт |
| «зачем каждый раз преобразовываться из `recipeRows`» | `recipes-store.ts` (`recipes`) | Маппинг row→domain вынесен в `toRecipe`, результат кэширует `computed` |
| «столько раз создаёшь ненужные массивы, написать отдельную функцию» | `grocery-store.ts` (`list`) | `toGroceryItem` + `groupGroceryItemsByList` |
| «упростить, много лишних массивов и итераций» | `weeks-store.ts` (`saveWeek`) | Один `reduce` вместо map + concat + sort |
| «объединить в один reduce» | `weeks-store.ts` (`addMeal`) | `reduce` по индексу `mealRowsByWeekDay` |
| «нужен рабочий подход для установки измерений» (2 места) | `recipes-store.ts`, `grocery-store.ts` | `features/recipes/lib/units.ts` — единый источник: `toIngredientUnit`, `unitLabel`, `INGREDIENT_UNITS`. Туда же схлопнуты дубли подписей «г/мл» из `unit-toggle.tsx` и `grocery-item-card.tsx` |
| «это что такое, сложно и непонятно» (2 места) | `recipes-store.ts` (`saveRecipe`) | `sync/write.ts`: `buildUpdate` (в БД летят только изменённые поля) и `diffById` (удалить / вставить / обновить). Тот же примитив применён в `resizeWeek` |
| «выведем ошибку в консоль, а пользователю ничего не скажем» | `recipes-store.ts` (`watch`) | `errors.notify` не только логирует — ставит `message`, который рисует `ErrorBanner` в руте; туда идут все `watch` и все `root.write` |
| «почему нет обработок ошибок (все места с await)» | `root-store.ts` (`seedDefaults`) | `await`-ы обёрнуты try/catch в вызывающем `onAuthChange`. Реальная дыра была одна — восстановление сессии молча падало в консоль; теперь `auth.init` репортит в `ErrorsStore` |
| «как получать логи при выполнении действия? очень много `write`» | `grocery-store.ts` над `generate` | `src/lib/log.ts` (`start`/`done`/`fail`, полные логи под `__DEV__`, в проде только `fail`). `RootStore.write` берёт thunk `() => Promise`, а не готовый промис — теперь меряется реальная длительность; третьим аргументом идёт `meta` (id, счётчики). Все 18 вызовов переведены на thunk |
| «почему нет аборта запроса при долгом ответе или закрытии приложения» | `grocery-store.ts` над классом | Таймаут сети уже был глобальный (`fetchWithTimeout`, 15 с в `lib/supabase.ts`) — покрывает auth и коннектор. Добавлен примитив `Scope.wait(promise)` + `ScopeClosed`: бросает, если scope закрылся до или после `await`; `ErrorsStore.notify` глотает `ScopeClosed` без баннера. Через него идут `auth.init`, `onAuthChange`, `seedDefaults`; в `waitForFirstSync` проброшен `scope.signal` — раньше он висел вечно без сети. Записи (`root.write`) сознательно не гасим: локальный SQLite, отмена = потеря данных, а `dispose()` при kill приложения и не вызывается |
| «почему `toISOString`, а не андроид?» (2 места) | `recipes-store.ts`, `grocery-store.ts` | Замечание — вопрос, дефекта нет. `toISOString` — ECMA-262, его даёт движок JS (Hermes/JSC), а не Android SDK; платформозависим только `Intl`, и он в проекте лишь для отображения. Формат обязан быть UTC фиксированной ширины: `created_at` в локальной БД — `column.text` (у SQLite типа даты нет), а сортируем строкой (`order by created_at`), плюс на сервере `timestamptz` парсит ISO-8601 с `Z` однозначно. Календарные даты (`DateId`, `YYYY-MM-DD`) намеренно отделены в `features/weeks/lib/dates.ts`. Намерение названо явно: `nowIso()` в `sync/write.ts`, все 6 мест переведены |
| «что за магия — если не массив, вернуть пустой?» | `mappers.ts` (`parseStringArray`) | Вопрос, кода не трогали (выбран вариант «только ответ»). SQLite массивов не знает → `photos`/`week_ids` лежат текстом (`schema.ts`), на сервере это `jsonb`, коннектор их разворачивает. Данные приезжают синком с других устройств и старых версий, поэтому `JSON.parse` может бросить или вернуть не массив — падать при отрисовке чужой строки нельзя, отсюда `[]`. **Осталось осознанно:** проверяется только контейнер, не элементы — `[1,2]` пройдёт как `string[]`; последствия были бы в `RecipeCollage` (`key`/`Image source`) и в `Set`/`includes` по `weekIds` (молча пустой список) |
| «что ты здесь пытаешься решить?» (`slugify`) | `mappers.ts` | Не решал ничего: механизм слотов держится на slug дефолтных категорий (`breakfast`/`lunch`/`dinner`/`snack`, сеются из `DEFAULT_CATEGORIES`), а slug пользовательской категории ни с одним из четырёх не совпадёт. При этом он создавал расхождение данных: на сервере `unique (owner_id, slug) where not deleted`, «Десерт» и «десерт!» дают один slug → `23505` → код попадает в `FATAL_CODES` коннектора и операция молча выбрасывается. `slugify` удалён, пользовательская категория пишет `slug = id` (uuid уникален). Заодно убрано приведение `as MealCategory`: `features/day-card/lib/meal-category.ts` → `toMealCategory` сверяет значение со списком из четырёх, `mealCategoryOf` вернул `MealCategory \| null`, `DayCardRecipe.category` стал nullable |
| «зачем `ingredients.entries()`, это уже массив» | `recipes-store.ts` (`addRecipe`) | Вопрос, кода не трогали. `.entries()` нужна не для итерации, а ради индекса — он пишется в колонку `position` (порядок ингредиентов). Обычный `for...of` индекса не даёт. Цена — итератор и пара на элемент, при том что рядом на каждый элемент идёт `await tx.execute`. Тот же приём осознанно оставлен в `root-store.ts` (`DEFAULT_CATEGORIES`) и `weeks-store.ts` (`DEFAULT_SLOTS`) — стиль общий |
| «для чего нужен `startWatching`, что он делает?» | `grocery-store.ts` | Не переименовывали — имя отражает `powersync.watch`. Ответ записан в `docs/sync-plan.md` (этап 4.4): единственный мост SQLite → MobX, вызывается тремя сторами из `RootStore.init()`, PowerSync через `EXPLAIN QUERY PLAN` находит исходные таблицы и перезапускает read-запрос при их изменении, результат идёт в `runInAction` в observable-строки; подписки живут на `Scope` и гаснут в `dispose()`. Обратный путь — `root.write` → SQLite → watch срабатывает сам |
| «не лишний ли поиск, если всё равно потом `powersync.execute`?» | `grocery-store.ts` (`toggleItem`) | Лишний. `find` шёл по всем `itemRows` (watch читает все строки, без фильтра по `list_id`) ради одного значения `checked`, а охранник `if (!item) return` ничего не давал — `update ... where id = ?` на несуществующем id просто трогает 0 строк. Инверсию делает SQL: `set checked = 1 - checked`. NULL взяться неоткуда — на сервере `checked boolean not null default false`, локально пишем `0/1`. Побочный плюс: значение считается от строки в БД, а не от снапшота MobX |
| «зачем писать здесь текст, это типа команда?» | `grocery-store.ts` (`setItemAmount`) | Да, команда: `powersync.execute(sql, params)` — SQL для локального SQLite, массив подставляется в `?` по порядку. Это параметры подготовленного выражения, не конкатенация: инъекция невозможна, план кешируется. Билдера нет, потому что ORM в проекте нет — PowerSync даёт голый SQLite; так же собирает SQL `sync/write.ts`. `edited = 1` литералом, потому что здесь всегда 1 — пометка «сумму правил пользователь», её рисует звёздочкой `grocery-item-card.tsx`, а при пересборке списка `buildGroceryList` ставит новым позициям `edited: false` |
| «что за кейс, когда и `userId`, и `connectedUserId` не определены — он валиден?» | `root-store.ts` (`onAuthChange`) | Валиден, кода не трогали. Кейса два: холодный старт — `reaction` объявлена с `fireImmediately: true`, а `auth.init` запущен без ожидания, так что первая реакция приходит, пока `getSession()` ещё в полёте; и повторный `null` после выхода — `else if (this.connectedUserId)` защищает от второго `disconnectAndClear`, который чистит локальную БД. Сюда же попадает пользователь, который никогда не входил. Обе ветки не выполняются, действий не требуется |
| «для чего используется `Haptics.impactAsync`» | `weeks-store.ts` | Тактильный отклик вибромотором. Было 9 вызовов четырёх разных API, размазанных по сторам и компонентам, все — «висячие» промисы без `catch`. Собрано в `src/lib/haptics.ts`: `created` / `removed` / `toggled` / `succeeded`, rejection гасится. Заодно учтена рекомендация SDK (expo-haptics 57.0.1, док к `impactAsync`): на Android `Vibrator` не рекомендован, вместо него `performAndroidHapticsAsync`, которому не нужен пермишен `VIBRATE` — обёртка ветвится по `Platform.OS`. Соответствия эффектов на Android (`Confirm` / `Toggle_Off` / `Segment_Tick`) выбраны по описаниям из типов SDK, на устройстве не проверялись; на старых API-уровнях часть эффектов может быть no-op |
| «где нормальная валидация почты?» | `app/sign-in.tsx` | Её не было — `canSubmit` смотрел только на непустоту, мусорный ввод уходил в сеть. Дыры «молча ничего» не было (Supabase возвращал ошибку в `AuthResult.message`), но цена плохая: лишний round-trip с 15-секундным таймаутом и английский серверный текст в русской форме (`toMessage` отдаёт `cause.message` как есть). Добавлены `EMAIL_PATTERN` + `emailValid` в `canSubmit` (кнопки не пускают неверный адрес) и `emailError` — подсказка «Проверь адрес почты» под полем, как только поле не пустое |
| «здесь очень сложно, что ты решаешь?» (`canSubmit`) | `app/sign-in.tsx` | Условие отвечает на один вопрос — можно ли нажать: почта валидна, пароль не пуст, запрос не в полёте. Разложено на `filled` и `canSubmit = filled && !busy`. Настоящая путаница была не в булеве: вторая кнопка тоже `disabled={!canSubmit}`, но её стиль про это не знал (`secondaryButton(pressed)` без `enabled`) — «Создать аккаунт» выглядела активной и не нажималась. Теперь обе кнопки гаснут одинаково |
| «почему нет обработки ошибок?» | `app/sign-in.tsx` (`submit`) | Обработка была: `AuthStore.request` ловит `try/catch` и возвращает `{ ok: false, message }`, экран рисует это над кнопками; `signIn`/`signUp` не бросают, так что `try` вокруг `submit` ничего бы не поймал. Дыра была в тексте — переводилась только сеть, остальное уходило английской фразой от GoTrue («Invalid login credentials» в русской форме). Теперь `toMessage` разбирает `AuthError.code` (стабильные машинные коды SDK) по карте `AUTH_MESSAGES`: неверные данные, неподтверждённая почта, занятый адрес, слабый пароль, рейт-лимиты, отключённая регистрация; неизвестный код → «Не удалось выполнить запрос». **Решено по ходу:** фолбэк `cause.message` оставлен для не-auth ошибок, чтобы баннер синка не потерял детали. Плюс `request` переведён с `console.error` на `log` из #1 — auth попал в общий поток логов с длительностью |
| «зачем два обновления стейта, что значит `busy`?» | `app/sign-in.tsx` (`submit`) | Обновления не подряд, а по разные стороны `await`: до запроса — заблокировать кнопки и стереть прошлую ошибку, чтобы старый текст не висел над новым запросом; после ответа — снять блокировку и записать результат. `busy` значил «запрос в полёте» и через `canSubmit` защищал от двойного тапа. Проблема была в моделировании: `busy: boolean` + `error: string \| null` дают 4 комбинации при трёх осмысленных. Заменено на `FormStatus = idle \| sending \| { error, message }` — блокировка и текст ошибки стали производными от одного состояния, невозможных комбинаций больше нет |
| «в чём смысл `ready`, почему в `finally`?» | `app/_layout.tsx` | Флаг значил «сессию проверили», а не «сессия есть»: `getSession()` читает сторедж асинхронно, и до ответа `session === null` неотличим от «не залогинен» — без флага `Stack.Protected` смонтировал бы `sign-in` и тут же увёл на `(tabs)`, залогиненный увидел бы вспышку чужого экрана. В `finally`, потому что при упавшем `getSession` флаг обязан подняться всё равно, иначе `return null` навсегда и белый экран без выхода; саму ошибку показывает баннер через `errors.notify`. Переименован в `sessionChecked` — имя больше не читается как «приложение готово». **Не сделано:** `SplashScreen.preventAutoHideAsync` в проекте нет, поэтому во время проверки виден пустой экран вместо сплэша |
| «`buildSourceHash` слишком сложна, зачем столько сортировок» (4 места) | `features/grocery/lib/build-list.ts` | Сортировки нужны: хеш обязан зависеть от содержимого, а не от порядка, а порядок недетерминирован — недели/дни/приёмы приходят как их отдал SQLite, рецепты по `created_at`, ингредиенты по `position, created_at`. Без них перестановка блюда внутри дня дала бы другой хеш при тех же данных. Сортировок три, потому что уровня вложенности три: приёмы, ингредиенты внутри рецепта, рецепты между собой. По перфу проблемы нет — считается по нажатию кнопки, не в рендере. **Настоящая проблема была другая:** хеш писался в `grocery_lists.source_hash`, читался обратно в `list.sourceHash` и **ни разу не сравнивался** — механизм был мёртвый, как `slugify` в #5. Теперь он работает: `GroceryStore.isStale` (computed) сверяет сохранённый хеш с пересчитанным по текущим неделям и рецептам, кнопка «Обновить» получает точку-индикатор и другой accessibility-label, когда список разошёлся с данными |
| «не дорого ли каждый раз создавать `new Map` и `new Set`?» | `features/grocery/lib/build-list.ts` | Не дорого — наоборот, это экономия. Замер на объёме 112 приёмов / 60 рецептов × 8 ингредиентов: `Map + Set` — 0.0443 мс за вызов, тот же обход через `find + some` — 0.6337 мс, разница в 14 раз (V8/Node на десктопе, не Hermes на устройстве — соотношение сохранится, абсолютные числа будут другие). Структуры строятся за один проход, дальше поиск O(1) вместо O(n·m). Вызывается по нажатию «Собрать»/«Обновить», не в рендере. После #17 `buildSourceHash` стал считаться чаще — в `computed` `isStale` при изменении недель или рецептов; MobX кеширует, пока значение наблюдают, это осознанная цена фичи «список устарел» |
| «что здесь происходит, намудрил» (эффект выбора недель) | `features/grocery/components/week-pick-sheet.tsx` | Эффект делает предвыбор при открытии шита: приоритет — недели из прошлого списка, иначе текущая календарная. Эффектом, а не `useState`, потому что шит смонтирован постоянно и только переключает `visible`. Была не только сложность, но и дефект: `weeks` и `list` — MobX `computed`, отдают новые ссылки при каждом пересчёте, поэтому эффект перезапускался при любом изменении данных и **затирал выбор пользователя, пока шит открыт** (отметил неделю → пришёл синк → отметки сбросились). Теперь предвыбор считается только на переход «закрыт → открыт» через `useRef`, зависимости остались полными (без `eslint-disable`), а сама логика вынесена в чистую `initialPick` в `features/grocery/lib/pick-weeks.ts` — один проход вместо двух `filter` |
| «зачем `todayId` создавать два раза» | `features/grocery/components/week-pick-sheet.tsx` | Второй вызов был не нужен: эффект выполняется после рендера, значит переменная из рендера доступна ему через замыкание. Считается один раз, используется и в предвыборе, и в пометке «текущая». `todayId()` возвращает строку-примитив, поэтому в зависимостях эффекта сравнивается по значению и лишних перезапусков не даёт |

**Новые файлы:** `src/stores/mappers.ts`, `src/sync/write.ts`, `src/features/recipes/lib/units.ts`, `src/features/day-card/lib/servings.ts` (константы порций переехали из стора в feature-lib по паттерну проекта), `src/lib/log.ts`, `src/features/day-card/lib/meal-category.ts`, `src/lib/haptics.ts`, `src/features/grocery/lib/pick-weeks.ts`.

---

## Ждёт

Пусто — все 20 замечаний закрыты, комментарии из кода удалены.

Открытым остался один вопрос, поднятый по ходу разбора #10 и не заведённый как пункт: в `RootStore.onAuthChange` поле `connectedUserId` присваивается **до** `await connect`, поэтому упавший `connect` оставляет id выставленным, и повторная реакция с тем же `userId` уходит в ранний выход — переподключения не будет до перезапуска приложения. Заводить пунктом или нет — решение за автором.

### Не трогать

`features/day-card/components/day-card-header.tsx:26` — личная пометка автора («этот комент не трогай, он для меня»).

---

## Порядок

~~Сквозные (1–3)~~ → ~~сторы (4–11)~~ → ~~экраны и логика (12–20)~~. Разбор завершён.
