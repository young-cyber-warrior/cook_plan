# Карта замечаний по коду

Замечания оставлены комментариями на русском прямо в коде. Правило работы: берём **одно** замечание, чиним, потом удаляем его комментарий, потом следующее.

Статусы: **закрыто** — фикс в коде, комментарий удалён · **в работе** · **ждёт** — комментарий на месте.

---

## Закрыто

| Замечание | Где было | Что сделано |
|---|---|---|
| «зачем столько геттеров, каждый раз запускается расчёт… (это касается всех сторов)» | `recipes-store.ts` над классом | Геттеры под `makeAutoObservable` уже `computed` — кэш есть, пока значение кто-то наблюдает (observer/reaction). Настоящие дыры закрыты: слой индексов-`computed` (`recipeRowById`, `ingredientsByRecipeId`, `itemRowsByListId`, `weekRowById`, `mealRowsByWeekDay`); экшены (`saveRecipe`, `refresh`, `generate`, `resizeWeek`) читают индексы, а не тяжёлые доменные массивы; `hasEdits` — `some` по группе вместо map+sort всего списка; `findBySlotCategory` — `find` по rows вместо построения Map всех рецептов с `JSON.parse` фото |
| «каждый раз при обращении к `hasEdits` проход по массиву» | `grocery-store.ts` | Считается по строкам активного списка, без сборки доменного `list` |
| «упростить всё в один цикл + вынести в функцию» | `recipes-store.ts` (`categories`) | `toCategories(rows, ownerId)` в `stores/mappers.ts` |
| «зачем все эти операции с `ingredientsByRecipe`» | `recipes-store.ts` (`recipes`) | `groupIngredientsByRecipe` — один проход строит группы, дальше O(1) на рецепт вместо фильтра по всем ингредиентам на каждый рецепт |
| «зачем каждый раз преобразовываться из `recipeRows`» | `recipes-store.ts` (`recipes`) | Маппинг row→domain вынесен в `toRecipe`, результат кэширует `computed` |
| «столько раз создаёшь ненужные массивы, написать отдельную функцию» | `grocery-store.ts` (`list`) | `toGroceryItem` + `groupGroceryItemsByList` |
| «упростить, много лишних массивов и итераций» | `weeks-store.ts` (`saveWeek`) | Один `reduce` вместо map + concat + sort |
| «объединить в один reduce» | `weeks-store.ts` (`addMeal`) | `reduce` по индексу `mealRowsByWeekDay` |
| «нужен рабочий подход для установки измерений» (2 места) | `recipes-store.ts`, `grocery-store.ts` | `features/recipes/lib/units.ts` — единый источник: `toIngredientUnit`, `unitLabel`, `INGREDIENT_UNITS`. Туда же схлопнуты дубли подписей «г/мл» из `unit-toggle.tsx` и `grocery-item-card.tsx` |
| «это что такое, сложно и непонятно» (2 места) | `recipes-store.ts` (`saveRecipe`) | `sync/write.ts`: `buildUpdate` (в БД летят только изменённые поля) и `diffById` (удалить / вставить / обновить). Тот же примитив применён в `resizeWeek` |
| «выведем ошибку в консоль, а пользователю ничего не скажем» | `recipes-store.ts` (`watch`) | `errors.notify` не только логирует — ставит `message`, который рисует `ErrorBanner` в руте; туда идут все `watch` и все `root.write` |
| «почему нет обработок ошибок (все места с await)» | `root-store.ts` (`seedDefaults`) | `await`-ы обёрнуты try/catch в вызывающем `onAuthChange`. Реальная дыра была одна — восстановление сессии молча падало в консоль; теперь `auth.init` репортит в `ErrorsStore` |

**Новые файлы:** `src/stores/mappers.ts`, `src/sync/write.ts`, `src/features/recipes/lib/units.ts`, `src/features/day-card/lib/servings.ts` (константы порций переехали из стора в feature-lib по паттерну проекта).

---

## Ждёт

### Сквозные (чинить один раз для всех сторов)

| # | Где | Замечание | Предлагаемый фикс |
|---|---|---|---|
| 1 | `stores/grocery-store.ts:87` | «как получать логи при выполнении действия? очень много `write`, нужно понимать где что сломалось» | Единая точка логирования в `RootStore.write` / `ErrorsStore`: источник, аргументы, длительность, результат. Уровень включается флагом, в проде — только ошибки |
| 2 | `stores/grocery-store.ts:14` | «почему нет аборта запроса при долгом ответе или закрытии приложения — это очень важно» | `Scope` уже есть и гасит `watch` по `signal`. Дыра — записи и `await` в экшенах: пробросить `scope.signal` / проверку `scope.closed` через `root.write` |
| 3 | `stores/recipes-store.ts:129`, `stores/grocery-store.ts:113` | «почему `toISOString`, а не андроид?» | Ответ + одна функция `nowIso()`: ISO-8601 UTC — формат сравним строкой, одинаков на обеих платформах, совпадает с `timestamptz` в Postgres |

### Сторы

| # | Где | Замечание | Предлагаемый фикс |
|---|---|---|---|
| 4 | `stores/mappers.ts:28` | «что за магия — если не массив, вернуть пустой?» | Колонка `photos` / `week_ids` — текст, `JSON.parse` может вернуть что угодно или бросить. Сделать контракт явным: отфильтровать не-строки |
| 5 | `stores/mappers.ts:15` | «что ты здесь пытаешься решить?» (`slugify`) | Slug категории для стабильного сопоставления со слотами дня (`breakfast`/`lunch`/…). Либо объяснить, либо убрать slug и хранить тип слота явным полем |
| 6 | `stores/recipes-store.ts:152` | «зачем `ingredients.entries()`, это уже массив» | Нужен индекс для `position`; заменить на обычный `for` с индексом |
| 7 | `stores/grocery-store.ts:23` | «для чего нужен `startWatching`, что он делает?» | Подписка на реактивные запросы PowerSync: результат SQL → observable-строки стора. Переименовать понятнее и/или описать в `docs` |
| 8 | `stores/grocery-store.ts:173` | «не лишний ли поиск, если всё равно потом `powersync.execute`?» | Убрать поиск: `update grocery_items set checked = 1 - checked where id = ?` |
| 9 | `stores/grocery-store.ts:188` | «зачем писать здесь текст, это типа команда?» | Это SQL-строка запроса, а не «текст». Вопрос снимается объяснением |
| 10 | `stores/root-store.ts:59` | «что за кейс, когда и `userId`, и `connectedUserId` не определены — он валиден?» | Да: холодный старт до восстановления сессии и состояние после выхода. Сейчас обе ветки просто ничего не делают |
| 11 | `stores/weeks-store.ts:235` | «для чего используется `Haptics.impactAsync`» | Тактильный отклик на действие. Решить: оставить, вынести в одну обёртку или убрать |

### Экраны и логика

| # | Где | Замечание | Предлагаемый фикс |
|---|---|---|---|
| 12 | `app/sign-in.tsx:24` | «где нормальная валидация почты?» | Проверка формата перед отправкой + текст ошибки под полем |
| 13 | `app/sign-in.tsx:25` | «здесь очень сложно, что ты решаешь?» (`canSubmit`) | Разложить условие или вынести в `isFormReady` |
| 14 | `app/sign-in.tsx:27` | «почему нет обработки ошибок?» | Ошибка приходит в `AuthResult.message` и рисуется в форме; при этом сетевые падения стоит отделить от неверного пароля |
| 15 | `app/sign-in.tsx:29` | «зачем два обновления стейта, что значит `busy`?» | `busy` — блокировка кнопки на время запроса. Свести к одному состоянию формы (`idle` / `sending` / `error`) |
| 16 | `app/_layout.tsx:23` | «в чём смысл `ready`, почему в `finally`?» | Флаг «сессия проверена»: до него нельзя решать, куда пускать — иначе кадр экрана входа мигает залогиненному. В `finally`, чтобы разблокировать UI и при ошибке |
| 17 | `features/grocery/lib/build-list.ts:30,39,52,57` | «`buildSourceHash` слишком сложна, зачем столько сортировок» | Сортировки нужны, чтобы хеш не зависел от порядка строк из БД. Свести к одной сортировке итогового массива частей |
| 18 | `features/grocery/lib/build-list.ts:76` | «не дорого ли каждый раз создавать `new Map` и `new Set`?» | Оценить на реальных объёмах: список строится по кнопке, не на каждый кадр |
| 19 | `features/grocery/components/week-pick-sheet.tsx:28` | «что здесь происходит, намудрил» (эффект выбора недель) | Предвыбор: прошлый выбор, иначе текущая неделя. Вынести в чистую функцию и считать при открытии |
| 20 | `features/grocery/components/week-pick-sheet.tsx:40` | «зачем `todayId` создавать два раза» | Считать один раз на рендер |

### Не трогать

`features/day-card/components/day-card-header.tsx:26` — личная пометка автора («этот комент не трогай, он для меня»).

---

## Порядок

Сквозные (1–3) → сторы (4–11) → экраны и логика (12–20).
